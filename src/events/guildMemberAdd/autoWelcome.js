const { Client, GuildMember } = require("discord.js");
const AutoWelcome = require("../../models/AutoWelcome");

/**
 * @param {Client} client
 * @param {GuildMember} member
 */
module.exports = (client, member) => {};
